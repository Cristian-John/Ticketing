node_modules
npm-debug.log
dist
.git
.gitignore
.npmrc
.env
.vscode

# Database files (persist via docker volume)
*.db
*.db-shm
*.db-wal

# Uploads (persist via docker volume)
uploads

# TypeScript sources are needed for builder, excluded from final image by multi-stage Dockerfile

# Audio files not used in code
notifsound.wav
severealarm.wav

# Docker files
Dockerfile
docker-compose.yml
nginx.conf
.dockerignore
